import { createMuiTheme } from "@mui/material/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#323232",
    },
    secondary: {
      main: "#9c27b0",
    },
    white: {
      main: "#fff",
    },
  },
});

export default theme;
